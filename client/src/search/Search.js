import React, { useState } from "react";

export default function Search() {
    const data = (type, name, page) => { };

    return (
        <div className="Search">
            <h1 className="headings">Search</h1>
            <hr />
        </div>
    );
}
